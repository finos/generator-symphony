host: <%= host %>

bot:
  username: <%= username %>
  privateKey:
    path: <%= privateKeyPath %>

ssl:
  trustStore:
    path: <%= trustStorePath %>

app:
  appId: <%= appId %>
  privateKey:
    path: <%= privateKeyPath %>
